#include<iostream>
using namespace std;

int main() {

	long long int num;
	cin >> num;
	long long int numOfInteration = num * (num - 1) * (num - 2) / 6;
	cout << numOfInteration << "\n";
	cout << 3;
}